import '../../styles/Status.css';

const Loading = () => {
    return (
        <div className="page-loader">
            <p>טוען נתונים, נא להמתין...</p>
        </div>
    );
}

export default Loading;